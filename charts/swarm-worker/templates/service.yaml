apiVersion: v1
kind: Service
metadata:
  name: {{ include "swarm-worker.fullname" . }}
  labels:
    {{- include "swarm-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.prometheus.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "swarm-worker.selectorLabels" . | nindent 4 }}
